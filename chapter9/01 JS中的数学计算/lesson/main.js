// MAX_VALUE 属性值接近于 1.79E+308
// 正无穷大

// Infinity
// console.log(Infinity)
// Math 就是一个内置对象 不是函数对象

// 属性
// Math.PI

// 方法

// console.log(Math.min(100,300,600,2))

// console.log(Math.random())

// 0 - 10 整数

// 5 - 15

// console.log(parseInt(Math.random() * 10 + 5))  // 5 -> 15


// console.log(Math.round(1.49))
/* 
Math.min( )

Math.max( )

Math.random( )  包括0  不包括1

// 这三个都是取整数
Math.floor( ) 小于或等于一个给定数字的最大整数 向下取整  地板

Math.ceil( ) 大于或等于一个给定数字的最小整数 向上取整   天花板

Math.round( )  四舍五入

Math.abs( )  取绝对值

**Math.pow()** 函数返回基数（`base`）的指数（`exponent`）次幂

**Math.sqrt() 开平方**

*/

// console.log(Math.sqrt(100) )

// sin cos tan 

// JS数学计算的精度问题
console.log(0.1 + 0.2)
console.log(1.1 * 100)
// toFixed

// 定义一个函数  传入两个参数 限定一个范围 都是正整数
// 会返回这个限定范围内的一个任意正整数 